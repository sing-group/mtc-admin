import React, { Component } from 'react';

import { List, Datagrid, TextField, EditButton, TextInput, SimpleForm, Create } from 'admin-on-rest';

import { Card, CardText, CardActions, CardHeader } from 'material-ui/Card';
import Paper from 'material-ui/Paper';


class CustomLayout extends Component {
    constructor(props) {
        super(props)
    }
    render(){

    }
}

const MultiLanguageRender = ({ map, language}) => (
    <div style={{ style }}>
    </div>
);